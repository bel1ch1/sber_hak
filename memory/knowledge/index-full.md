# Knowledge Base Index

- **git_checkout_ambiguity**: commit_reviewed fails with 'git checkout ouroboros' when a directory named ouroboros/ exists alongside a tracking branch
- **heal_mode_constraints**: Repair tasks operate in heal_mode with restricted tool access: only skill_payload inspection (list_files, read_file) and
- **improvement-backlog**: This topic stores concrete, evidence-backed improvement items discovered during task execution.
- **onboarding_ambiguity_lessons**: Краткие запреты после e2e. Оркестратор и этапные skills обязаны учитывать.
- **onboarding_capability_map**: Статус: 2026-07-19. Почта и календарь — **только Google** (`gmail`, `google_calendar`). Yandex mail/calendar MCP отключе
- **onboarding_pipeline_rules**: Статус: единый процессный контракт. Оркестратор `skills/onboarding` и все этапные skills обязаны ему следовать.
- **ouroboros_mcp_tasks**: Агент пишет: `[CAPABILITY_OMISSION_MANIFEST] mcp: ephemeral_turn` — MCP tools недоступны.
- **patterns**: | Error class | Count | Root cause | Structural fix | Status |
- **safety_check_model_errors**: Safety supervisor (`ouroboros/safety.py`) always calls **LIGHT** model with `reasoning_effort="none"`.
- **yandex_calendar**: Yandex Calendar MCP create_event: if the model sends both end and duration_minutes, the server now prefers duration_minu
