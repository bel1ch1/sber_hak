Yandex Calendar MCP create_event: if the model sends both end and duration_minutes, the server now prefers duration_minutes and ignores end (warning both_end_and_duration_given). Default duration is 60 minutes when neither is set. Verify creation with list_events in the same window.
