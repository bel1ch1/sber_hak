[project]
name = "wiki-mock-mcp"
version = "0.1.0"
description = "Mock company wiki MCP for Ouroboros onboarding"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.6,<2.0",
]

[tool.uv]
package = false
