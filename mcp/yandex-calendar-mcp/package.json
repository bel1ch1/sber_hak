{
  "name": "yandex-calendar-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "with-ca.cmd --import tsx server.ts",
    "server:http": "with-ca.cmd --import tsx server.ts --http",
    "smoke": "with-ca.cmd --import tsx smoke-yandex-calendar.ts",
    "smoke:discovery": "with-ca.cmd --import tsx smoke-discovery-only.ts",
    "test": "node --import tsx --test --test-reporter=spec yandex-calendar.test.ts yandex-calendar-schemas.test.ts server-factory.test.ts http-transport.test.ts",
    "capture": "with-ca.cmd --import tsx save-create-capture.ts",
    "build:check": "npx esbuild server.ts --bundle --platform=node --target=node24 --format=esm --outfile=.build-check/server.mjs --packages=external && echo build:check ok"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cheerio": "^1.2.0",
    "ical.js": "^2.0.1",
    "tsdav": "^2.1.4",
    "tsx": "^4.20.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
